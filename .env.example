# Backend API (no trailing slash)
VITE_API_URL=http://localhost:5000

# Google Maps — browser key (HTTP referrer must include this admin origin).
# Enable: Places API (New), Geocoding API. Used for vendor shop location search (no map).
# VITE_GOOGLE_MAPS_API_KEY=
